networks:
  mangabaka-network:
    driver: bridge

services:
  postgresql:
    container_name: mangabaka-postgresql
    image: postgres:alpine3.16
    environment:
      POSTGRES_DB: ${PG_DB_NAME}
      POSTGRES_PASSWORD: ${PG_PASSWORD}
      POSTGRES_USER: ${PG_USERNAME}
    ports:
      - "5432:5432"
    volumes:
      - ../../backend/src/main/resources/docker/sql:/docker-entrypoint-initdb.d
    networks:
      - mangabaka-network
    restart: always

  app:
    build:
      context: ../..
      dockerfile: docker/prod/Dockerfile
    container_name: mangabaka
    environment:
      PG_PASSWORD_PROD: ${PG_PASSWORD}
      PG_USERNAME_PROD: ${PG_USERNAME}
      PG_JDBC_URL_PROD: ${PG_JDBC_URL}
    depends_on:
      - postgresql
    ports:
      - "9090:9090"
    networks:
      - mangabaka-network
    restart: always
